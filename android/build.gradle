plugins {
    id 'java-library'
    id 'maven-publish'
}

group = 'com.apiverve'
version = '1.1.13'

java {
    sourceCompatibility = JavaVersion.VERSION_1_8
    targetCompatibility = JavaVersion.VERSION_1_8
}

dependencies {
    implementation 'org.json:json:20231013'
}

publishing {
    publications {
        maven(MavenPublication) {
            groupId = 'com.apiverve'
            artifactId = 'randomusergenerator-api'
            version = '1.1.13'

            from components.java

            pom {
                name = 'Random User Generator'
                description = 'Random User Generator is a simple tool for generating random user data. It returns the user's name, email, and more.'
                url = 'https://apiverve.com'

                licenses {
                    license {
                        name = 'MIT License'
                        url = 'https://opensource.org/licenses/MIT'
                    }
                }

                developers {
                    developer {
                        id = 'apiverve'
                        name = 'APIVerve'
                        email = 'hello@apiverve.com'
                    }
                }

                scm {
                    connection = 'scm:git:git://github.com/apiverve/randomusergenerator-api.git'
                    developerConnection = 'scm:git:ssh://github.com/apiverve/randomusergenerator-api.git'
                    url = 'https://github.com/apiverve/randomusergenerator-api'
                }
            }
        }
    }
}
